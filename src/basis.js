export class Basis {

    constructor(options) {
        this.id = options.id
        this.width = options.width
        this.height = options.height
        this.posX = options.posX
        this.posY = options.posY
    }

}